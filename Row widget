import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({Key? key}) : super(key: key); // ✅ key added

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      debugShowCheckedModeBanner: false,
      home: RowExample(),
    );
  }
}

class RowExample extends StatelessWidget {
  const RowExample({Key? key}) : super(key: key); // ✅ key added

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Row widget example"),
        backgroundColor: const Color.fromARGB(255, 126, 196, 250),
      ),

      body: Row(
        children: [
          // A
          Expanded(
            child: Container(
              height: 150,
              color: Colors.blue,
              child: const Center(
                child: Text(
                  "A",
                  style: TextStyle(fontSize: 24, color: Colors.black),
                ),
              ),
            ),
          ),

          // B
          Expanded(
            child: Container(
              height: 150,
              color: Colors.amber,
              child: const Center(
                child: Text(
                  "B",
                  style: TextStyle(fontSize: 24, color: Colors.black),
                ),
              ),
            ),
          ),

          // C
          Expanded(
            child: Container(
              height: 150,
              color: Colors.red,
              child: const Center(
                child: Text(
                  "C",
                  style: TextStyle(fontSize: 24, color: Colors.black),
                ),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
